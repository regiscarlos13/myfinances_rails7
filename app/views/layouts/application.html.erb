<!DOCTYPE html>
<html  lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Sistema de Finanças Pessoais</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <%= render partial: 'layouts/shared/header' %>
    <%= render partial: 'layouts/shared/message' %>
    <div class="container-fluid">
      <div class="row">
        <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
          <div class="position-sticky pt-3">
            <ul class="nav flex-column">
              <li class="nav-item">
                <li class="nav-item">
                  <%= link_to  root_path, class: 'nav-link active' do%>
                    <span class='bi bi-house-door'> Dashboard</span>
                  <% end %>
                </li>
              </li>
              <%= render 'layouts/shared/menu' %>
            </div>
          </nav>
          <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
              <h1 class="h2"><%= @card_title %></h1>
              <div class="btn-toolbar mb-2 mb-md-0">
                <div class="btn-group me-2">
                  <%= @btn_new %>
                </div>
              </div>
            </div>
            <%= yield %>
          </main>
        </div>
      </div>
    </body>
    
    </html>
